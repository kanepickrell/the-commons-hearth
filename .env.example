# Copy to .env.local (gitignored) for local dev. Vercel gets the same two vars
# in Project → Settings → Environment Variables.
#
# The publishable key is safe to ship in the browser bundle — Row-Level
# Security decides what it can read. Never put the secret/service-role key here.
VITE_SUPABASE_URL=https://ppzgdwjojztugeyqvbdi.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_...
